% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-rect-fade.R
\name{hatch}
\alias{hatch}
\title{Hatch pattern for \code{geom_rect_fade()}}
\usage{
hatch(
  angle = 45,
  style = c("parallel", "crossed"),
  spacing = grid::unit(2, "mm")
)
}
\arguments{
\item{angle}{Line angle in degrees (default \code{45}).}

\item{style}{\code{"parallel"} (default) draws a single family of parallel lines;
\code{"crossed"} overlays a second family at \code{angle + 90} (a crosshatch when
diagonal, a grid when axis-aligned).}

\item{spacing}{Distance between adjacent lines, as a \code{\link[grid:unit]{grid::unit()}}
(default \code{unit(2, "mm")}). A bare number is treated as millimetres.}
}
\value{
A \code{ggpointless_pattern} object to pass to the \code{pattern} argument of
\code{\link[=geom_rect_fade]{geom_rect_fade()}}.
}
\description{
\code{hatch()} builds a line-hatch specification to pass to the \code{pattern} argument
of \code{\link[=geom_rect_fade]{geom_rect_fade()}}. The lines are drawn at a true visual angle, clipped to
each rectangle, and faded along with the alpha gradient. A single helper
covers the common CAD-style fills:
\describe{
\item{diagonal stripe}{\code{hatch()} (the default, 45 degrees)}
\item{diagonal crosshatch}{\code{hatch(style = "crossed")}}
\item{vertical stripe}{\code{hatch(90)}}
\item{horizontal stripe}{\code{hatch(0)}}
\item{square grid}{\code{hatch(0, style = "crossed")}}
}
}
\details{
Line spacing is a physical distance (millimetres), so it stays constant when
the plot is resized rather than scaling with the panel.

Hatch line colour always follows the \code{fill} aesthetic -- a fixed colour
independent of the fill would break the mapping. Line weight and dash
pattern are fixed at sensible defaults (\code{linewidth = 0.5}, solid lines)
and are not exposed as parameters for the same reason.
The hatch transparency is owned by the fade (\code{alpha} / \code{alpha_fade_to} on
the geom), so \code{hatch()} has no \code{alpha} argument.
}
\examples{
library(ggplot2)

df <- data.frame(xmin = 1, xmax = 5, ymin = 0, ymax = 4)
base <- ggplot(df) +
  aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax)

# Diagonal stripe (default), faded toward the baseline
base + geom_rect_fade(fill = "tomato", alpha = 0.9, pattern = hatch())

# Crosshatch at a custom angle
base + geom_rect_fade(
  fill = "steelblue", pattern = hatch(30, style = "crossed")
)

# Square grid with wider spacing
base + geom_rect_fade(
  pattern = hatch(0, style = "crossed", spacing = unit(4, "mm"))
)
}
\seealso{
\code{\link[=geom_rect_fade]{geom_rect_fade()}}
}
\concept{fading gradient}
