# stat_chaikin: ratio > 0.5 warns (complement conversion)

    ! `ratio` = 0.7 is outside the conventional Chaikin range `[0, 0.5]`; flipping to its complement 0.3.
    i For closed paths the resulting vertex set is the same (with a cyclic shift); for open paths the curve differs from the one implied by `ratio = 0.7`.
    i For stronger smoothing, increase `iterations` instead.

