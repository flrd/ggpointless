% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/position-ridgeline.R
\name{position_ridgeline}
\alias{position_ridgeline}
\title{Position for Ridgeline Plots}
\usage{
position_ridgeline(scale = 1, min_height = 0)
}
\arguments{
\item{scale}{Height multiplier applied to \code{height}. Values > 1 increase
overlap between adjacent ridges. Defaults to \code{1}.}

\item{min_height}{Minimum \code{height} value to draw. Points with
\code{height < min_height} are dropped, creating gaps in the ridgeline.
Defaults to \code{0}.}
}
\value{
A \link[ggplot2:Position]{ggplot2::Position} ggproto object.
}
\description{
\code{position_ridgeline()} computes the ribbon bounds (\code{ymin}, \code{ymax}) that
\code{\link[=geom_ridgeline_fade]{geom_ridgeline_fade()}} requires. Each ridge sits at its own \code{y}-baseline;
\code{ymin} is set to \code{y} and \code{ymax} to \code{y + scale * height}. Points whose
\code{height} falls below \code{min_height} are turned into \code{NA} gaps.

This position is set automatically by \code{\link[=geom_ridgeline_fade]{geom_ridgeline_fade()}} and should
rarely need to be called directly.
}
